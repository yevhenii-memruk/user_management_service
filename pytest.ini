[pytest]

pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

filterwarnings =
    ignore::DeprecationWarning:passlib.*

markers =
    unit: Unit tests that don't require database or API connections
    integration: Tests that require API connections
    functional: End-to-end functional tests
